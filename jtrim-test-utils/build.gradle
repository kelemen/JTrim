apply plugin: 'jtrim-java-internal'

dependencies {
    compile project(':jtrim-logs')
    compile project(':jtrim-concurrent')

    // TODO: Move version numbers to a common location
    compile 'junit:junit:4.11'
    compile 'org.mockito:mockito-core:1.9.5'
}
