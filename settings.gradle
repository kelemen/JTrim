def subDirs = rootDir.listFiles(new FileFilter() {
    public boolean accept(File file) {
        return file.isDirectory();
    }
});

subDirs.each { File dir ->
    String dirName = dir.name;
    if (dirName.toLowerCase(Locale.US).startsWith("jtrim")) {
        include dirName;
    }
}
