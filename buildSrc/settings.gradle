rootProject.name = 'jtrim-buildsrc'

enableFeaturePreview('VERSION_CATALOGS')

def buildJavaVersion = providers.gradleProperty('buildJavaVersion').getOrElse('17')

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            version('java', buildJavaVersion)
        }
    }
}
