apply plugin: 'java';

sourceCompatibility = '1.7';

repositories {
    mavenCentral();
}

dependencies {
    compile project(':jtrim-core');
    testCompile group: 'junit', name: 'junit', version: '4.10';
}
